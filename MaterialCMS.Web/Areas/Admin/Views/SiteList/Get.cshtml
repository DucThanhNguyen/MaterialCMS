@using MaterialCMS.Website
@model System.Collections.Generic.IList<MaterialCMS.Entities.Multisite.Site>

<ul class="dropdown-menu site-list" role="menu">
    @foreach (var site in Model)
    {
        <li><a href="@site.GetFullDomain">@site.Name</a></li>
    }
</ul>

